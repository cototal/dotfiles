alias rails-s='bundle exec rails server -b 0.0.0.0'
alias rails-c='bundle exec rails c'

function b-rails() {
  bundle exec rails $*
}
function b-rake() {
  bundle exec rake $*
}

function s-rails() {
  RAILS_ENV=staging rails $*
}
